# Infrastructure is a transient program

Files are the greatest, and earliest 

Imagine that, instead of writing the following Python program:

```python
require 'uuid'
uuid.UUID()
```

```
$ require 'uuid'
$ print(uuid.UUID())
```

## Prior art

[Common Lisp, Smalltalk]
